# 疯狂奶茶杯

基于 Pygame 的脑机接口（BCI）奶茶制作游戏。玩家通过键盘或头环控制奶茶杯，接住从天而降的食材，完成一杯杯奶茶。通过科创平台实时获取专注力和头动数据，专注力影响食材速度和收益系数，最终累积营业额提升等级。

---

## 目录

- [项目结构](#项目结构)
- [快速开始](#快速开始)
- [主菜单与控制模式](#主菜单与控制模式)
- [游戏控制](#游戏控制)
- [车道系统](#车道系统)
- [热身阶段](#热身阶段)
- [正式游戏机制](#正式游戏机制)
- [记忆模式](#记忆模式)
- [脑机接口（BCI）系统](#脑机接口bci系统)
- [信号滤波与映射算法](#信号滤波与映射算法)
- [音频系统](#音频系统)
- [账户与存档系统](#账户与存档系统)
- [历史记录系统](#历史记录系统)
- [配置参数表](#配置参数表)
- [打包部署](#打包部署)

---

## 项目结构

```
BCI_game/
├── main.py                         # 游戏主入口，状态机管理界面跳转
├── config.py                       # 全局配置（分辨率、食材属性、BCI参数、等级等）
├── requirements.txt
├── CrazyMilkTea.spec               # PyInstaller 打包配置
│
├── menu/                           # 菜单系统
│   ├── login.py                    # 登录/注册界面
│   ├── splash.py                   # 启动动画
│   ├── summary.py                  # 游戏结算界面（含专注力波形图）
│   ├── components.py               # 基础组件（MenuItem, Badge, ClickParticle）
│   ├── mode_selector.py            # 模式选择器（辉光粒子风格）
│   ├── bci_button.py               # 通用辉光按钮（GlowButton）
│   ├── history.py                  # 历史记录界面（左右分栏+趋势曲线）
│   ├── particles.py                # 菜单粒子效果
│   └── screens/
│       ├── main_menu.py            # 主菜单（3 按钮布局）
│       └── game_settings.py        # 游戏设置
│
├── game/                           # 游戏核心
│   ├── session.py                  # 常规/BCI/键盘模式游戏会话（热身+一杯制主循环）
│   ├── memory_mode.py              # 记忆模式游戏会话
│   ├── sprites.py                  # 精灵（杯子、食材、粒子、特效）
│   ├── ingredient_manager.py       # 食材生成管理（等级/概率/速度/车道占用）
│   ├── hud.py                      # 游戏 HUD（背景板、数据面板、进度条）
│   ├── cup_manager.py              # 杯生命周期管理器 + 难度自适应
│   ├── patience_bar.py             # 耐心条（已废弃）
│   └── font_utils.py               # 中文字体加载
│
├── bci/                            # 脑机接口模块
│   ├── data_reader.py              # TCP 连接科创平台，解析注意力/陀螺仪/焦点数据
│   ├── filter.py                   # 信号滤波器 + 注意力映射曲线
│   └── config.py                   # BCI 连接配置
│
├── data/                           # 数据管理
│   ├── score_manager.py            # 得分/金钱管理
│   ├── player_profile.py           # 玩家档案（等级、营业额、历史、增删查）
│   ├── memory_recipes.py           # 记忆模式配方库（2~5食材组合，35个配方）
│   └── ingredient_config.py        # 食材属性配置
│
├── core/                           # 核心框架
│   ├── state_machine.py            # 通用状态机
│   └── audio_manager.py            # 音频管理
│
├── assets/
│   ├── images/
│   │   ├── ingredients/            # 18 种食材图片（含秘方和冰块）
│   │   ├── cups/                   # 奶茶杯图片
│   │   ├── badges/                 # 4 等级徽章图片
│   │   ├── backgrounds/            # 背景图片
│   │   └── other/                  # 其他素材
│   ├── fonts/                      # ZCOOLKuaiLe-Regular.ttf
│   └── sounds/                     # 音频文件
│
├── profiles/                       # 各用户游戏存档（JSON）
└── accounts.json                   # 账号密码存储
```

---

## 快速开始

```bash
pip install -r requirements.txt
python main.py
```

启动流程：**启动画面 → 登录/注册 → 主菜单 → 选择控制模式 → 开始游戏**

---

## 主菜单与控制模式

主菜单包含 3 个按钮布局：游戏开始、模式选择、游戏设置。

### 控制模式

| 模式               | 说明                                                                           | 底层游戏配置                                    |
| ------------------ | ------------------------------------------------------------------------------ | ----------------------------------------------- |
| **常规模式** | BCI 头环控制杯子移动。点击开始后尝试连接 BCI，连接失败弹窗提示后降级为键盘控制 | 使用 BCI 游戏底层（36杯、自由搭配、热身→正式） |
| **记忆模式** | 记忆配方食材序列，然后按序接住（详见下方）                                     | 独立游戏逻辑，不经过热身阶段                    |
| **键盘模式** | 纯键盘控制，不连接 BCI。注意力默认 50，**跳过热身**，直接进入正式游戏    | 使用 BCI 游戏底层（无热身、无归一化）           |

### BCI 未连接时的行为

- 常规模式 BCI 失败 → `control_mode = "bci_failed"` → **跳过热身**，直接进入正式游戏
- 键盘模式 → `control_mode = "keyboard"` → **跳过热身**，直接进入正式游戏
- 对局**不保留历史记录**，得分**不计入等级累计**
- 音效正常工作（接食材、必接、结算、升级等）

---

## 游戏控制

| 操作         | 方式                                                              |
| ------------ | ----------------------------------------------------------------- |
| ← / →      | 键盘左右移动杯子                                                  |
| ESC          | 返回菜单 / 双击跳过结算                                           |
| 头环         | BCI 模式下头部转动控制杯子（取科创平台 `focus_x` 作为屏幕坐标） |
| F11          | 全屏/窗口切换                                                     |
| 右上角 □/❐ | 点击最大化/恢复窗口                                               |

---

## 车道系统

### 六车道配置

画面分为 **6 条垂直车道**，每条宽约 213px（1280÷6）：

```
┌──────┬──────┬─────────────┬──────┬──────┐
│ 车道0 │ 车道1 │  车道2+3合并  │ 车道4 │ 车道5 │
│ 掉落  │ 掉落  │  无分割线    │ 掉落  │ 掉落  │
│ 小料  │ 小料  │  不掉落食材  │ 小料  │ 小料  │
└──────┴──────┴─────────────┴──────┴──────┘
```

- **中间两条车道（车道2、3）**：合并为一个宽区域，无分割线，**不掉落食材**
- **左右四条车道（车道0、1、4、5）**：随机掉落食材
- 每条车道最多同时有 1 个食材（由 `_free_lanes()` 占用检测保证）

### 车道占用检测算法

仅检查屏幕上方 35% 区域的食材，底部即将消失的食材不阻塞新生成：

```
occupied = set()
for ing in ingredients:
    if ing.rect.y < SCREEN_HEIGHT * 0.35:     # 仅上方区域
        lane = min(ing.centerx // LANE_WIDTH, 5)  # clamp 防越界
        occupied.add(lane)
free_lanes = [l for l in [0, 1, 4, 5] if l not in occupied]
```

### 食材生成位置

随机从空闲车道中选取一条，在该车道宽度范围内随机生成 X 坐标：

```
lane = random.choice(free_lanes)
x = random.randint(lane * LANE_WIDTH, (lane + 1) * LANE_WIDTH - INGREDIENT_SIZE)
```

---

## 热身阶段

进入正式游戏前，先进入 **3 分钟热身阶段**（`WARMUP_DURATION = 180`），目的是收集玩家的注意力数据，用于正式游戏的个性化难度归一化。

> **注意**：键盘模式和 BCI 失败模式（`control_mode = "keyboard"` 或 `"bci_failed"`）**跳过热身阶段**，直接进入正式游戏，归一化使用默认范围 `[30, 70]`。

### 热身流程

```
热身介绍(3s) → 热身游戏(3min) → 热身总结(5s) → 正式游戏
```

### 热身玩法

- 与正式游戏相同的接食材玩法，**不计分、不设杯子系统**
- 食材持续掉落，玩家用杯子接住
- 屏幕顶部显示"热身阶段"和倒计时 `MM:SS`

### 热身速度调节（3 秒平滑窗口）

取最近 3 秒注意力滑动平均值，映射到 3 个速度档位：

| 注意力范围（平滑后） | 速度                       | 含义           |
| -------------------- | -------------------------- | -------------- |
| 0 ~ 15               | 冻结                       | 画面和时间停止 |
| 15 ~ 46              | `WARMUP_SPEED_MAX` (6.0) | 快速（不专注） |
| 47 ~ 72              | 中速 (3.75)                | 中等           |
| 73 ~ 100             | `WARMUP_SPEED_MIN` (1.5) | 慢速（高专注） |

速度变化幅度大（1.5 ~ 6.0），**感知明显**，鼓励玩家尽快找到专注状态。

### 低注意力冻结机制

- **冻结触发**：实际注意力值**持续 5 秒 ≤ 10** → 画面和时间冻结，显示"请调整身心状态"
- **解冻恢复**：注意力**持续 3 秒 > 15** → 恢复正常游戏和倒计时
- 冻结期间杯子仍可移动，食材和倒计时暂停

### 热身结束 — 归一化基准计算

热身结束后，取**最后 30 秒**注意力数据，计算个性化归一化范围：

```
上界 = 最后30秒最高注意力（上限 100）
下界 = 最后30秒平均注意力 - 25（下限 0）
范围 = [下界, 上界]

若 上界 - 下界 < 10：向两侧各扩展 5，保证范围宽度至少为 10
若 无有效数据：默认范围 [30, 70]
```

将热身阶段导出的范围映射到 1-100，作为正式游戏的**归一化基准**：

```
归一化值 = clamp((实时注意力 - 下界) / (上界 - 下界) × 99 + 1, 1, 100)
```

---

## 正式游戏机制

### 一杯制规则

- 一局最大 **12 分钟（720 秒）**，共 **36 杯**，每杯最长 **20 秒**（`CUP_DURATION = 20`）
- **杯结束结算逻辑**（`CupManager.settle_cup()`）：

  1. 无必接食材 → 本杯金额 = 0
  2. 有必接食材 → 本杯金额 = 所有接住食材分值之和
  3. 触发秘方 → 本杯金额 × 2（触发即翻倍，弹窗提示后自动生效）

### 食材分值表

| 食材       | 分值 | 食材         | 分值 | 食材   | 分值 |
| ---------- | ---- | ------------ | ---- | ------ | ---- |
| 珍珠       | 1    | 椰果         | 1    | 牛奶   | 2    |
| 红茶       | 2    | 绿茶         | 2    | 芋圆   | 2    |
| 脆啵啵     | 2    | 芒果         | 3    | 椰奶   | 3    |
| 草莓       | 3    | 芋泥         | 3    | 燕麦奶 | 4    |
| 咖啡       | 4    | 特调稀奶油顶 | 5    | 米酿   | 5    |
| 咸芝士奶盖 | 6    | 茉莉花茶     | 6    | 冰块   | 0    |

### 专注力收益系数（BCI 模式）

防止低专注力下食材速度快反而接到更多、收益更高。高专注力 → 全额收益，低专注力 → 收益打折：

```
系数(norm) = ┌ 1.0                        , norm ≥ 80
             │ 0.5 + 0.5 × (norm-40)/40   , 40 ≤ norm < 80
             └ 0.5                        , norm < 40
```

| 归一化值 | 系数      | 含义             |
| -------- | --------- | ---------------- |
| ≥ 80    | 1.0       | 全额收益         |
| 40 ~ 80  | 0.5 ~ 1.0 | 线性过渡         |
| < 40     | 0.5       | 惩罚（最低保障） |

杯结算时：`最终金额 = 原始金额 × 系数`

### 正式游戏速度调节（归一化线性映射）

正式游戏使用热身阶段导出的归一化范围，将实时注意力动态映射到食材速度：

```
速度 = FORMAL_SPEED_MAX - (归一化值 - 1) / 99 × (FORMAL_SPEED_MAX - FORMAL_SPEED_MIN)
     = 4.5 - (归一化值 - 1) / 99 × 2.5
```

| 归一化值      | 速度                   | 含义 |
| ------------- | ---------------------- | ---- |
| 1（低专注）   | FORMAL_SPEED_MAX (4.5) | 较快 |
| 50            | 3.25                   | 中等 |
| 100（高专注） | FORMAL_SPEED_MIN (2.0) | 较慢 |

速度变化范围 2.0 ~ 4.5（差 2.5），远小于热身阶段的 1.5 ~ 6.0（差 4.5），变化感知减轻。

### 生成间隔动态调节

生成间隔随速度自适应，使食材密度保持一致：

```
速度比 = 当前速度 / 基础速度
调整后间隔 = 基础间隔 × (0.7 + 0.6 × 速度比)
最终间隔 ∈ [0.3, 3.0]
```

### 4 级食材与等级系统

| 等级 | 升级条件    | 可用食材（含前级）              | 必接食材（含前级） |
| ---- | ----------- | ------------------------------- | ------------------ |
| Lv.1 | 默认        | 珍珠、椰果、牛奶、红茶、绿茶    | 牛奶、红茶、绿茶   |
| Lv.2 | 累计 ≥ 80  | +芋圆、脆啵啵                   | +芒果、椰奶        |
| Lv.3 | 累计 ≥ 250 | +草莓、芋泥                     | +燕麦奶、咖啡      |
| Lv.4 | 累计 ≥ 600 | +特调稀奶油顶、米酿、咸芝士奶盖 | +茉莉花茶          |

每级保留所有前级食材，等级越高可选种类越多。

### 冰块替换机制（注意力方差自适应）

以**上一杯奶茶的平均专注力**为基线（第1杯使用热身阶段最后30秒均值），计算实时注意力与基线的偏离方差：

```
offset_i = 实时注意力_i - 上杯基线
方差 = Σ(offset_i - offset̄)² / N     （60帧滑动窗口）
```

**每杯独立基线计算**：杯结束后取该杯所有注意力样本的均值，作为下一杯的基线。

根据方差动态决定冰块替换概率，方差越大 → 冰块越频繁：

| 方差     | 模式     | 冰块概率 | 含义                                   |
| -------- | -------- | -------- | -------------------------------------- |
| < 50     | 简单模式 | 20%      | 注意力稳定，平均 5 个小料中 1 个变冰块 |
| 50 ~ 150 | 中等模式 | 50%      | 正常波动，平均 2 个小料中 1 个变冰块   |
| > 150    | 困难模式 | 80%      | 波动大，大多数小料被替换为冰块         |

**特殊规则**：方差 ≥ 150 且实时注意力 < 20 → 冰块概率 100%（只掉冰块）。

**冰块机制**：冰块**不是额外掉落**，而是在正常小料生成管线中按概率**替换**原定食材。冰块分值 0，不参与必接判定。玩家视角就是"小料变少，冰块变多"。

### 低专注保护（正式游戏）

- 注意力 **≤ 10 持续 5 秒** → 游戏暂停，半透明黑化遮罩，大字提示"请调整身心状态"
- 注意力 **> 15 持续 3 秒** → 恢复游戏
- 遮罩透明度以指数平滑过渡：`α += (target - α) × 0.05`

### 秘方触发机制

**BCI 模式** — 基于持续高专注触发：

```
第1杯阈值 = 热身最后30s平均专注力 + 10
第N杯阈值 = 第N-1杯平均专注力 + 10
若 注意力 > threshold 持续 4 秒 → 触发秘方！
```

**非 BCI 模式** — 基于杯序周期性触发：

- 每 `secret_recipe_cup_interval` 杯触发一次（常规模式每杯触发，BCI 模式每 3 杯触发）

**触发方式改为弹窗**（v2）：秘方不再作为掉落食材出现。触发时屏幕中央弹出半透明弹窗，显示"触发秘方！"文字和秘方图案（左右晃动 + 旋转），持续 2 秒。杯结算时**触发即翻倍**（不再需要接住秘方食材）。

### 防伪迹仲裁（BCI 模式）

正常专注时头部有 1-2 度自然微抖，若检测到异常（刻意咬牙、僵直）则触发惩罚：

- **检测条件**：陀螺仪三轴变化 **均 < 0.5°** 且专注力 **> 80**，持续 **5 秒**
- **惩罚**：屏幕提示"请放松面部肌肉"，画面和时间冻结 **5 秒**，自动恢复
- 恢复后重置陀螺仪基准，防止连续触发

### 最后 5 分钟专注力记录

每局游戏结束时，根据专注力采样和时间推算最近 5 分钟（300 秒）的平均专注力：

```
sps = len(focus_samples) / game_duration           # 每秒采样数
n = max(1, int(sps × 300))                          # 最后 300 秒的样本数
last_5min_avg = mean(focus_samples[-n:])             # 尾部样本均值
```

该值存入历史记录，作为趋势曲线的数据点。

---

## 记忆模式

### 游戏流程

```
[规则弹窗 3.5s] ← 整个游戏只出现一次
     ↓
┌── 循环 ───────────────────────────────────┐
│ [记忆阶段 2s]  食材图片并排显示 + 配方名    │
│ [接食材 15s]   5车道随机掉落，按序接食材    │
│ [结果显示 1.5s] 成功/失败判定              │
│ [休息 2s]      显示等级/进度统计           │
└──────────────────────────────────────────┘
```

### 核心规则

1. **记忆配方**：屏幕中央显示 2~5 种食材图片和配方名称，停留 2 秒
2. **按序接住**：食材消失后，所有食材随机在 5 个车道掉落，**必须按配方顺序依次接住**
3. **严格顺序**：接错顺序即本轮失败。15 秒内未接完所有配方食材 → 失败
4. **穿插投放**：配方食材按 `_recipe_ratio = 3` 的比例穿插在随机干扰食材中出现（每 3 个中有 1 个是目标食材）

### 难度进阶

| 等级 | 配方食材数 | 晋级/降级条件         |
| ---- | ---------- | --------------------- |
| Lv1  | 2          | 连续成功 3 次 → 升级 |
| Lv2  | 3          | 连续成功 3 次 → 升级 |
| Lv3  | 4          | 连续成功 3 次 → 升级 |
| Lv4  | 5（上限）  | 保持在最高级          |

- 失败 → 连续成功清零
- 累计失败 2 次 → 降一级 + 失败计数清零

### 车道生成算法

记忆模式使用**独立的 5 车道系统**（不同于主游戏的 6 车道），每条车道宽 256px：

```
lane = _free_lane()       # 随机选取空闲车道
center = lane × 256 + 128  # 车道中心
x = random(center-30, center+30)  # 微调 x 坐标
```

车道空闲判断：该车道内食材数 < `_max_per_lane`（当前为 1）且无食材 `y < _lane_spacing`（400px）。

### 关键参数

| 参数                    | 值           | 说明                    |
| ----------------------- | ------------ | ----------------------- |
| `_spawn_interval`     | 0.8s         | 生成间隔                |
| `_max_per_lane`       | 1            | 每车道最大食材数        |
| `_lane_spacing`       | 400          | 同车道垂直阻断间距      |
| `_ingredient_speed`   | 5.5 px/frame | 食材下落速度            |
| `_recipe_ratio`       | 3            | 目标食材:干扰食材 = 1:2 |
| `_memorize_time`      | 2s           | 记忆阶段时长            |
| `_rules_display_time` | 3.5s         | 规则弹窗时长            |
| `_drop_window`        | 15s          | 接食材阶段最大时长      |

---

## 脑机接口（BCI）系统

### 架构概览

```
[科创平台] ←→ [TCP Socket 127.0.0.1:8000] ←→ [BCIDataReader] ←→ [GameSession]
                              │
                   IPC 二进制协议
               (4字节大端长度 + JSON负载)
```

### 连接流程

1. 主菜单选择常规模式并点"游戏开始"
2. 尝试 TCP 连接科创平台（默认 `127.0.0.1:8000`）
3. 成功 → 发送就绪消息 → 启动陀螺仪焦点算法 → 进入热身阶段
4. 失败 → 弹窗提示"BCI头环未连接，将使用键盘控制"
5. 游戏过程中持续接收注意力 + 陀螺仪 + 焦点坐标数据

### IPC 通信协议

**发送格式**：4 字节大端长度 + UTF-8 JSON 负载

**就绪消息**：

```json
{"type": "ready", "client": "crazy_milk_tea_cup"}
```

**启动陀螺仪算法**：

```json
{
  "msg": "ipc_algorithm_start_test",
  "algorithm_name": "gyroscope",
  "algorithm_args": {
    "left": 0, "top": 0,
    "width": 1280, "height": 720,
    "sensitivityX": 8, "sensitivityY": 8
  }
}
```

**接收数据格式**：

```json
{
  "msg": "ipc_algorithm_test",
  "algorithm_name": "attention",
  "result_args": {"data": 75}
}
```

```json
{
  "msg": "ipc_algorithm_test",
  "algorithm_name": "gyroscope",
  "result_args": {
    "data": {
      "focus_x": 640, "focus_y": 360,
      "gyroscope_x": 9.5, "gyroscope_y": 258.0, "gyroscope_z": 176.0
    }
  }
}
```

### 数据读取机制

- **批量消费**：每帧处理缓冲区中**所有**到达的消息，只保留最新值，消除积压延迟
- **超时断开**：超过 2 秒未收到新数据 → 自动标记连接断开
- **阻塞分离**：socket 设为非阻塞，用 `BlockingIOError` 处理无数据情况
- **实时显示**：注意力数据不使用平滑，直接实时反映变化

---

## 音频系统

音频系统由 `core/audio_manager.py` 的 `AudioManager` 统一管理，支持背景音乐（BGM）和音效（SFX）两类播放。

### BGM 流转链

| 阶段       | 音乐               | 说明                                      |
| ---------- | ------------------ | ----------------------------------------- |
| 程序启动   | `背景乐3.mp3`    | 程序启动即播放，覆盖登录界面              |
| 进入主菜单 | `玻璃糖果园.wav` | MenuState.enter() 切换                    |
| 过场动画   | `晨光木盒.wav`   | stop_bgm() 停旧曲 → 播放 → 续用到游戏中 |
| 正式游戏   | (沿用晨光木盒)     | GameSession 不切换 BGM，从过场续用        |

### 音效事件

| 音效文件                  | 触发场景                         |
| ------------------------- | -------------------------------- |
| `音效/按键1.mp3`        | 主菜单按钮点击（开始/模式/设置） |
| `音效/按键2.mp3`        | ESC 退出 / 关闭窗口              |
| `音效/接到食材.wav`     | 接住普通食材（volume=0.2）       |
| `音效/接到必接食材.wav` | 接住必接食材（volume=0.3）       |
| `音效/漏接必接食材.wav` | 必接食材漏出屏幕                 |
| `音效/触发秘方.wav`     | 秘方弹窗触发                     |
| `音效/加金币.wav`       | 杯结算有收入                     |
| `音效/升级.wav`         | 等级提升                         |
| `音效/游戏结束.wav`     | 游戏结束                         |

### 技术实现

- SFX 使用 `pygame.mixer.Sound`，懒加载后缓存
- 最大 16 个同时音效通道（`set_num_channels(16)`）
- BGM 使用 `pygame.mixer.music`，循环播放
- 所有音效文件位于 `assets/sounds/音效/`

### 必接食材粒子效果

必接食材下落时附带金色粒子环绕（与秘方同样的参数：每帧 4 个，散布 20~40px，金色 `(255,215,0)`），视觉上与普通食材明显区分。粒子在食材图层下方渲染，不遮挡图片。

---

所有滤波器位于 `bci/filter.py`。

### 1. 死区滤波（DeadZoneFilter）

消除微小抖动信号，当输入绝对值 < 阈值时输出 0：

```
filter(x) = 0           , 若 |x| < threshold
          = x           , 否则
```

默认阈值 = 5.0。

### 2. 指数平滑（ExponentialSmoothing）

使信号变化更平滑，减少抖动：

```
y_0 = x_0                                            （首次直接取输入）
y_n = α × x_n + (1 - α) × y_{n-1}                   （迭代公式）
```

| α 值       | 响应速度 | 抖动程度 | 适用场景 |
| ----------- | -------- | -------- | -------- |
| 0.1         | 慢       | 非常平滑 | 极静环境 |
| 0.3（默认） | 中等     | 适度平滑 | 正常使用 |
| 0.7         | 快       | 微抖动   | 快速响应 |

### 3. 灵敏度曲线（SensitivityCurve）

非线性映射，小幅度更灵敏，大幅度更稳定：

```
y = sign(x) × base_sensitivity × |x| ^ exponent
```

默认参数：`base_sensitivity = 1.0, exponent = 1.5`

`exponent > 1` 时，小幅信号被压缩更多，需要更大动作才能触发大位移。

### 4. 专注力→速度曲线（AttentionToSpeedCurve）

3 段线性，以校准基线为参考：

```
速度(attn) = ┌ Vmax                            , attn ≤ baseline - 20
             │ Vmax - (Vmax-Vmin) × t          , baseline-20 < attn ≤ baseline+20
             └ Vmin                            , attn > baseline + 20
                          其中 t = (attn - lo) / (hi - lo)
```

默认参数：`speed_min = 2.0, speed_max = 5.0, baseline = 60.0`

专注力越高 → 食材越慢 → 更易接住；专注力越低 → 食材越快。

### 5. 难度自适应（DifficultyAdapter）

根据 30 秒滚动窗口内的平均专注力动态调整难度基线，同时结合**逐杯基线**（每杯独立计算均值作为下杯参考）：

```
samples = [(t, attention), ...]                       # 保留最近 30 秒样本
avg = mean(samples)                                   # 窗口均值
baseline = clamp(avg, DIFFICULTY_BASELINE_MIN, DIFFICULTY_BASELINE_MAX)
```

基线用于秘方触发阈值计算：`secret_threshold = baseline + 10`

**逐杯基线**（`_update_attention_variance` 方差计算的偏移基准）：

```
第1杯基线 = warmup最后30s平均专注力（无则40）
第N杯基线 = 第N-1杯所有注意力样本的均值
```

---

## 账户与存档系统

- 首次启动需创建账号密码（存于 `accounts.json`）
- 每个账号独立存档（`profiles/用户名.json`）
- JSON 存档格式：

```json
{
  "level": 2,
  "cumulative_revenue": 156,
  "total_games": 5,
  "games_history": [
    {
      "mode": "bci",
      "revenue": 42,
      "cups": 36,
      "secrets": 2,
      "avg_attention": 65.3,
      "last_5min_avg_attention": 72.1,
      "duration": 645.2,
      "date": "2026-06-03 14:30",
      "focus_samples": [50, 52, 55, ...]
    }
  ]
}
```

### 等级阈值

| 等级 | 累计营业额门槛 |
| ---- | -------------- |
| Lv.1 | 0              |
| Lv.2 | 80             |
| Lv.3 | 250            |
| Lv.4 | 600            |

### 存档操作

- `add_game_result()` — 添加对局记录，自动累加营收、检查升级
- `remove_game(idx)` — 删除单条记录，自动重算营收和等级
- `clear_history()` — 清空所有记录，重置统计
- `save()` — 持久化到 JSON，保留最近 20 条记录
- `_recalculate()` — 重算 total_games、cumulative_revenue、level
- 只保留最近 20 条历史记录

---

## 历史记录系统

主菜单点击徽章进入历史记录界面。

### 操作功能

- **左键**点击列表项 → 弹出详情窗口（含游戏数据 + 专注力波形图）
- **右键**点击列表项 → 弹出确认框 → 删除该条记录
- **一键清除**按钮（标题右侧，小号按钮）→ 弹出确认框 → 清空全部历史
- **滚轮**翻页浏览
- 删除后自动重算等级并即时保存

### 专注力波形图

详情窗口中绘制单局专注力随时间变化的波形图：

- 采样降频至最多 150 个点（分桶取均值）
- X 轴：时间（0s ~ 游戏时长）
- Y 轴：专注力（0 ~ 100）
- 网格线每 25 单位

---

## 配置参数表

常用可调参数在 `config.py`：

### 车道与画面

| 参数                        | 默认值       | 说明                    |
| --------------------------- | ------------ | ----------------------- |
| `SCREEN_WIDTH`            | 1280         | 画面宽度                |
| `SCREEN_HEIGHT`           | 720          | 画面高度                |
| `NUM_LANES`               | 6            | 车道总数                |
| `LANE_WIDTH`              | 213          | 每条车道宽度（1280÷6） |
| `MIDDLE_LANE_INDICES`     | [2, 3]       | 中间合并车道索引        |
| `INGREDIENT_LANE_INDICES` | [0, 1, 4, 5] | 掉落食材的车道索引      |

### 一杯制

| 参数             | 默认值 | 说明                 |
| ---------------- | ------ | -------------------- |
| `CUP_DURATION` | 20     | 每杯时间上限（秒）   |
| `TOTAL_CUPS`   | 36     | 总杯数（最大12分钟） |

### 热身阶段

| 参数                     | 默认值 | 说明                     |
| ------------------------ | ------ | ------------------------ |
| `WARMUP_DURATION`      | 180    | 热身时长（秒）           |
| `WARMUP_LOW_THRESHOLD` | 10     | 冻结注意力阈值           |
| `WARMUP_FREEZE_TIME`   | 5.0    | 低专注持续多久冻结（秒） |
| `WARMUP_RESUME_TIME`   | 3.0    | 恢复后多久解冻（秒）     |
| `WARMUP_SMOOTH_WINDOW` | 3.0    | 速度平滑窗口（秒）       |
| `WARMUP_SPEED_MIN`     | 1.5    | 热身最低速度             |
| `WARMUP_SPEED_MAX`     | 6.0    | 热身最高速度             |

### 正式游戏

| 参数                 | 默认值 | 说明                   |
| -------------------- | ------ | ---------------------- |
| `FORMAL_SPEED_MIN` | 2.0    | 最低速度（归一化=100） |
| `FORMAL_SPEED_MAX` | 4.5    | 最高速度（归一化=1）   |

### 防伪迹仲裁

| 参数                             | 默认值 | 说明                     |
| -------------------------------- | ------ | ------------------------ |
| `ARTIFACT_STILL_THRESHOLD`     | 0.5    | 陀螺仪静止判定阈值（度） |
| `ARTIFACT_STILL_DURATION`      | 5.0    | 静止持续时间（秒）       |
| `ARTIFACT_ATTENTION_THRESHOLD` | 80     | 专注力异常阈值           |
| `ARTIFACT_PENALTY_DURATION`    | 5.0    | 惩罚冻结时长（秒）       |

### 游戏模式

| 参数                                       | 默认值 | 说明                   |
| ------------------------------------------ | ------ | ---------------------- |
| `GAME_MODES["bci"].spawn_interval`       | 1200   | BCI 模式生成间隔（ms） |
| `GAME_MODES["bci"].ingredient_speed`     | 3      | BCI 模式基础速度       |
| `GAME_MODES["bci"].total_cups`           | 36     | BCI 模式杯数           |
| `GAME_MODES["regular"].ingredient_speed` | 3      | 常规模式基础速度       |

### 秘方

| 参数                      | 默认值 | 说明               |
| ------------------------- | ------ | ------------------ |
| `SECRET_RECIPE_SUSTAIN` | 4      | 秘方触发需持续专注秒数 |

### 难度自适应

| 参数                        | 默认值 | 说明             |
| --------------------------- | ------ | ---------------- |
| `DIFFICULTY_BASELINE`     | 60     | 难度基线初始值   |
| `DIFFICULTY_ADAPT_WINDOW` | 30     | 自适应窗口（秒） |
| `DIFFICULTY_BASELINE_MIN` | 40     | 基线调节下限     |
| `DIFFICULTY_BASELINE_MAX` | 80     | 基线调节上限     |

### BCI 连接

| 配置项                                 | 默认值        | 说明           |
| -------------------------------------- | ------------- | -------------- |
| `bci_config.json` → `server_ip`   | `127.0.0.1` | 科创平台地址   |
| `bci_config.json` → `server_port` | `8000`      | 科创平台端口   |
| `BCI_CONNECTION_TIMEOUT`             | 5             | 连接超时（秒） |

### 食材

| 参数                 | 默认值 | 说明                         |
| -------------------- | ------ | ---------------------------- |
| `INGREDIENT_SIZE`  | 80     | 食材显示尺寸（px）           |
| `INGREDIENT_SPEED` | 3.5    | 基础下落速度                 |
| `INGREDIENT_TYPES` | 18 种  | 所有食材种类（含秘方和冰块） |
| `INGREDIENT_TIERS` | 4 级   | 等级食材系统                 |

---

## 打包部署

使用 PyInstaller 打包为独立 `.exe`：

```bash
pyinstaller CrazyMilkTea.spec
```

打包配置（`CrazyMilkTea.spec`）：

- 主入口：`main.py`
- 包含 `assets/` 目录为数据文件
- 包含 `profiles/`、`bci_config.json`、`accounts.json` 为外部数据
- 不生成控制台窗口（`console=False`）
- 图标：`assets/images/other/游戏图标.png`

---

## 开发里程碑

- [X] 基础游戏框架 + 一杯制
- [X] 4 级食材 + 等级系统
- [X] BCI 头动控制
- [X] 登录/注册 + 存档
- [X] 专注力波形图（结算界面 + 历史详情）
- [X] 低专注保护暂停 + 防伪迹仲裁
- [X] 热身阶段（注意力收集 + 个性化归一化）
- [X] 正式游戏归一化线性速度映射
- [X] 六车道系统（中间双车道合并，不掉落食材）
- [X] 每车道最多 1 个小料限制
- [X] 12 分钟正式游戏（20s × 36 杯）
- [X] 专注力收益系数（专注意义化分段函数）
- [X] 3 信号滤波器（死区 + 指数平滑 + 灵敏度曲线）
- [X] 4 级食材 + 等级系统
- [X] 记忆模式（配方记忆 + 顺序验证 + 难度进阶）
- [X] 历史记录界面（左右分栏 + 趋势曲线 + 增删管理）
- [X] 最后 5 分钟平均专注力记录
- [X] 常规模式头环未连接不记录历史
- [X] 冰块替换机制（注意力方差→冰块概率）
- [X] 逐杯基线计算（每杯结束用该杯均值作为下杯基线）
- [X] 秘方弹窗机制（触发即翻倍，不再掉落秘方食材）
- [X] 道具粒子效果（必接食材金色粒子环绕）
- [X] 菜单按键音效 + 退出音效
- [X] 多场景背景音乐（登录/菜单/过场/游戏）
- [X] 音效系统（接食材/必接/漏接/秘方/加金币/升级/结束）
- [X] 无BCI模式跳过热身直接进入正式游戏
- [ ] 记忆模式 BCI 头环控制

---

## 许可证

仅供学习和研究使用。
